# Attribution Review Prompt

You are reviewing a Polygraph attribution packet. Decide whether GPT, Claude, Grok, or the user influenced the trade or decision.

Use only the packet context. Choose one allowed attribution value and return JSON matching the packet schema. Do not recommend new trades, orders, entries, exits, or live market action.
