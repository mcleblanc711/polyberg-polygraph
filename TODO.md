# TODO

- Before making further changes next session, test the MVP end to end:
  - Run `pytest`.
  - Start the Streamlit app.
  - Import `tests/fixtures/sample_trades.csv` through the UI.
  - Confirm duplicate imports are skipped.
  - Create a decision, link trades, add attribution, and export packets.
